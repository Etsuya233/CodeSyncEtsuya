#include <cstdio>

int main(){
	int l, n, k;
	scanf("%d%d%d", &l, &n, &k);
	int arr[n];
	for(int i = 0; i < n; i++){
		scanf("%d", &arr[i]);
	}
	int head = 0, foot = l + 1, half;
	while(head < foot){
		half = (head + foot) / 2;
		
		
	}
	
	
	
}
